[project]
name = "kindle-to-pdf"
version = "0.1.0"
description = "Capture Kindle Mac app pages as screenshots and compile them into a PDF"
requires-python = ">=3.12"
dependencies = [
    "pillow>=10.0.0",
]

[project.scripts]
kindle-to-pdf = "kindle_to_pdf:main"
